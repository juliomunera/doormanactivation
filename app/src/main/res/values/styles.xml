<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bottomNavigation</item>
        <item name="colorPrimaryDark">@color/toolBar</item>
        <item name="android:textColorPrimary">@color/focusIcon</item>
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/bottomNavigation</item>
        <item name="itemTextColor">@color/focusIcon</item>
        <item name="itemIconTint">@color/white</item>
    </style>
</resources>

